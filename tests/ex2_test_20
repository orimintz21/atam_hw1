.global _start

.section .text
    mov $destination, %rax
    cmpb $130, 0(%rax)
    jne bad_exit

    cmpb $247, 1(%rax)
    jne bad_exit

    cmpb $25, 2(%rax)
    jne bad_exit

    cmpb $118, 3(%rax)
    jne bad_exit

    cmpb $111, 4(%rax)
    jne bad_exit

    cmpb $99, 5(%rax)
    jne bad_exit

    cmpb $99, 6(%rax)
    jne bad_exit

    cmpb $202, 7(%rax)
    jne bad_exit

    cmpb $60, 8(%rax)
    jne bad_exit

    cmpb $215, 9(%rax)
    jne bad_exit

    cmpb $32, 10(%rax)
    jne bad_exit

    cmpb $159, 11(%rax)
    jne bad_exit

    cmpb $44, 12(%rax)
    jne bad_exit

    cmpb $10, 13(%rax)
    jne bad_exit

    cmpb $134, 14(%rax)
    jne bad_exit

    cmpb $42, 15(%rax)
    jne bad_exit

    cmpb $220, 16(%rax)
    jne bad_exit

    cmpb $124, 17(%rax)
    jne bad_exit

    cmpb $47, 18(%rax)
    jne bad_exit

    movq $60, %rax
    movq $0, %rdi
    syscall

bad_exit:
    movq $60, %rax
    movq $1, %rdi
    syscall

.section .data
num: .int -19
source: .byte 47,124,220,42,134,10,44,159,32,215,60,202,99,99,111,118,25,247,130,126,204,209,96,41,155,9,24,209,9,123,196
destination: .zero 58
