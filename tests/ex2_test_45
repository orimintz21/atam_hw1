.global _start

.section .text
    mov $destination, %rax
    cmpb $135, 0(%rax)
    jne bad_exit

    cmpb $89, 1(%rax)
    jne bad_exit

    cmpb $179, 2(%rax)
    jne bad_exit

    cmpb $58, 3(%rax)
    jne bad_exit

    cmpb $136, 4(%rax)
    jne bad_exit

    cmpb $193, 5(%rax)
    jne bad_exit

    cmpb $9, 6(%rax)
    jne bad_exit

    cmpb $31, 7(%rax)
    jne bad_exit

    cmpb $100, 8(%rax)
    jne bad_exit

    cmpb $19, 9(%rax)
    jne bad_exit

    cmpb $44, 10(%rax)
    jne bad_exit

    cmpb $100, 11(%rax)
    jne bad_exit

    cmpb $131, 12(%rax)
    jne bad_exit

    cmpb $23, 13(%rax)
    jne bad_exit

    cmpb $129, 14(%rax)
    jne bad_exit

    cmpb $96, 15(%rax)
    jne bad_exit

    movq $60, %rax
    movq $0, %rdi
    syscall

bad_exit:
    movq $60, %rax
    movq $1, %rdi
    syscall

.section .data
num: .int 16
source: .byte 135,89,179,58,136,193,9,31,100,19,44,100,131,23,129,96,72,106,134,196,225,101,18,219,48,170,39,22
destination: .zero 80
