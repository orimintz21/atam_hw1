.global _start

.section .text
    mov $destination, %rax
    cmpb $7, 0(%rax)
    jne bad_exit

    cmpb $191, 1(%rax)
    jne bad_exit

    cmpb $133, 2(%rax)
    jne bad_exit

    cmpb $129, 3(%rax)
    jne bad_exit

    cmpb $186, 4(%rax)
    jne bad_exit

    cmpb $167, 5(%rax)
    jne bad_exit

    cmpb $16, 6(%rax)
    jne bad_exit

    cmpb $200, 7(%rax)
    jne bad_exit

    cmpb $173, 8(%rax)
    jne bad_exit

    cmpb $223, 9(%rax)
    jne bad_exit

    cmpb $191, 10(%rax)
    jne bad_exit

    cmpb $193, 11(%rax)
    jne bad_exit

    cmpb $0, 12(%rax)
    jne bad_exit

    cmpb $244, 13(%rax)
    jne bad_exit

    cmpb $190, 14(%rax)
    jne bad_exit

    movq $60, %rax
    movq $0, %rdi
    syscall

bad_exit:
    movq $60, %rax
    movq $1, %rdi
    syscall

.section .data
num: .int 15
source: .byte 7,191,133,129,186,167,16,200,173,223,191,193,0,244,190,252,59,42,2,46,61,75,9,139,102,67
destination: .zero 57
